server:
  port: 8081

backend:
  url: ${BACKEND_URL:http://localhost:8080}

spring:
  thymeleaf:
    cache: false
    encoding: UTF-8
  web:
    resources:
      static-locations: classpath:/static/

logging:
  level:
    mx.unam.fesaragon.frontend: INFO
